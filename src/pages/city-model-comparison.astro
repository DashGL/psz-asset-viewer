---
import ModelComparisonScene from '../components/ModelComparisonScene';

const base = import.meta.env.BASE_URL;
const lndmdModelUrl = `${base}city_e/s00e_sa1/lndmd/s00e_sa1_m.glb`;
const fixedModelUrl = `${base}city_e/s00e_sa1_m.glb`;
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Model Comparison - PSZ Asset Viewer</title>
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      html, body {
        width: 100%;
        height: 100%;
        overflow: hidden;
      }

      body {
        background: #0a0a0a;
        color: #ffffff;
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
      }
    </style>
  </head>
  <body>
    <ModelComparisonScene
      client:only="react"
      lndmdModelUrl={lndmdModelUrl}
      fixedModelUrl={fixedModelUrl}
    />
  </body>
</html>
